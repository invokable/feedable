name: update

on:
  schedule:
    - cron: '0 4 * * *' #UTC

jobs:
  composer:
    uses: kawax/composer-workflow/.github/workflows/update.yml@b1ad2a4aa1d05fba8ce5c3590f486546a5c7c0fa # v1
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
  npm:
    uses: kawax/npm-workflow/.github/workflows/update.yml@0340b8e7582f60e440c100abca8c306ddf221fda # v1
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
